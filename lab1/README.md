# Iris Dataset Linear Regression and ANOVA Analysis

This Jupyter notebook performs exploratory data analysis (EDA), linear regression, and ANOVA on the Iris dataset.

## Requirements

Install dependencies in a Python virtual environment:

```bash
python -m venv .venv
source .venv/bin/activate  # On Windows: .\.venv\Scripts\activate
pip install -r requirements.txt
